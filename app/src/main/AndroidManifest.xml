<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.passagewell">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PassageWell">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ZhuceActivity"/>
        <activity android:name=".DengluActivity"/>
        <activity android:name=".SoloActivity"/>
        <activity android:name=".IndivadualActivity"/>
        <activity android:name=".BaseActivity"/>
        <service android:name=".TimuService" />
    </application>

</manifest>